Laravel PetStore ApiGenerator

The project can be mounted using docker:
- Start docker daemon
  
- Run:
  docker-compose up -d
  
- Web running on localhost:8088
  
- To stop de container:
  docker-compose stop

:)